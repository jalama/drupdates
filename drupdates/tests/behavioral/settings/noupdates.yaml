# Test successful if Drupdates runs and no updates are needed.
working_dirs:
  builds:
    dir: builds
repo_dirs:
  drupal:
    base: drupal7_current
    working_directory: builds
additional_settings:
  workingBranch:
    value: dev
  reportingTool:
    value: file
  reportingFormat:
    value: yaml
  fileName:
    value: report.yaml
