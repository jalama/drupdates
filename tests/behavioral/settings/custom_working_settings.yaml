# Test successful if the Git Log shows the update committed by Drupdates
repo_dirs:
   drupal:
     custom_settings: drupal_working
     base: drupal7_base
additional_settings:
  workingBranch:
    value: dev
